# File Server JS

**Build project**
```
./node_modules/.bin/tsc --w
```

**Run server locally**
```
npm start
```

**Test in web browser**
```
open http://localhost:3000/
````

**Deploy to Heroku**
```
git push heroku master
```